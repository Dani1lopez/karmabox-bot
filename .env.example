# =========================
# KarmaBox Bot (FastAPI)
# =========================

# --- Telegram ---
# Token del bot creado con @BotFather
TELEGRAM_BOT_TOKEN=REPLACE_ME

# --- Base URL pública (para webhooks: Telegram/WhatsApp) ---
# Si usas ngrok o despliegue, pon aquí la URL pública:
# Ej: https://xxxxx.ngrok-free.app  |  https://tu-app.onrender.com
API_BASE_URL=REPLACE_ME

# --- Google Sheets ---
# Ruta al JSON del Service Account (NO subir a git)
GOOGLE_SERVICE_ACCOUNT_FILE=secrets/service_account.json

# Nombre exacto del Google Sheet (debe coincidir 1:1)
SHEET_NAME=KarmaBox Leads

# --- Groq (Opcional) ---
# Si no lo usas, déjalo vacío
GROQ_API_KEY=
# Modelo por defecto (ajústalo si quieres)
AI_MODEL=llama-3.3-70b-versatile

# --- WhatsApp (Opcional / Bloqueado por Meta ahora mismo) ---
# Token de verificación para el webhook (tú lo eliges)
# Si no lo usas, puedes dejarlo vacío.
WHATSAPP_VERIFY_TOKEN=el_que_crea_tu

WHATSAPP_GRAPH_VERSION=version

WHATSAPP_PHONE_NUMBER_ID=number_id

WHATSAPP_ACCESS_TOKEN=tu_token

